\begin{lstlisting}[style=bookpython,caption={Planned code placeholder for biased selection and unbiased combine.},label={lst:ch09-biased-selection-and-unbiased-combine}]
def biased_selection_and_unbiased_combine_placeholder(x, config):
    """Chapter 9.3 placeholder.

    Planned purpose: Router forward pass returning topk_idx from biased scores and gates from unbiased selected scores.
    Replace this stub during section development.
    """
    raise NotImplementedError("Implement this section's code path.")
\end{lstlisting}
