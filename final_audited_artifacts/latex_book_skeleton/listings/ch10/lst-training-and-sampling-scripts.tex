\begin{lstlisting}[style=bookpython,caption={Planned code placeholder for training and sampling scripts.},label={lst:ch10-training-and-sampling-scripts}]
def training_and_sampling_scripts_placeholder(x, config):
    """Chapter 10.4 placeholder.

    Planned purpose: train.py main function with config loading, training loop, checkpointing, and sampling hook.
    Replace this stub during section development.
    """
    raise NotImplementedError("Implement this section's code path.")
\end{lstlisting}
