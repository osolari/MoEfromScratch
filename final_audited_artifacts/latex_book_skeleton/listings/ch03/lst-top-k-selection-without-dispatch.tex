\begin{lstlisting}[style=bookpython,caption={Planned code placeholder for top k selection without dispatch.},label={lst:ch03-top-k-selection-without-dispatch}]
def top_k_selection_without_dispatch_placeholder(x, config):
    """Chapter 3.3 placeholder.

    Planned purpose: torch.topk example that returns topk_score and topk_idx.
    Replace this stub during section development.
    """
    raise NotImplementedError("Implement this section's code path.")
\end{lstlisting}
