# Session starter: Chapter 3.4 - Naive token-by-token dispatch

Develop only Chapter 3.4, **Naive token-by-token dispatch**, for *MoE Models from Scratch*.

Use the detailed brief at:

`section_development_briefs/ch03/04-naive-token-by-token-dispatch.md`

Must produce LaTeX section prose at:

`latex_book_skeleton/chapters/ch03/sections/04-naive-token-by-token-dispatch.tex`

Must update artifacts only through standalone `.tex` wrappers:

- `\input{figures/ch03/fig-naive-token-by-token-dispatch}`
- `\input{tables/ch03/tab-naive-token-by-token-dispatch}`
- `\input{equations/ch03/eq-naive-token-by-token-dispatch}`
- `\input{listings/ch03/lst-naive-token-by-token-dispatch}`

Follow the section arc: problem opening -> visual anchor -> shapes/math -> implementation -> diagnostic -> bridge. Preserve the color map and artifact wrapper contract from the brief.
