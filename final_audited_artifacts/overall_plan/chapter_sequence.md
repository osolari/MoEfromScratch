# Chapter sequence

| Chapter | Part | Title | Role in build | Code output |
| --- | --- | --- | --- | --- |
| 1 | Part I - foundations and baseline | From dense compute to conditional compute | Motivate MoE from the dense FFN bottleneck and establish the build ladder. | Create chapter notebooks that calculate simple parameter/FLOP estimates and visualize token-to-expert routing with hand-built tensors. |
| 2 | Part I - foundations and baseline | A dense Transformer baseline from scratch | Build a minimal decoder-only language model before replacing the FFN with MoE. | Produce a dense decoder implementation, train loop, sample script, and validation-loss baseline. |
| 3 | Part II - routing mechanics from scratch | Experts, routers, and the first MoE layer | Introduce the MoE layer as a transparent replacement for the dense FFN. | Implement ExpertMLP, Router, and a naive TopKMoELayer that passes shape and gate checks. |
| 4 | Part II - routing mechanics from scratch | Top-1 routing and Switch-style dispatch | Build the simplest sparse router and expose the expert-load problem. | Implement Top1Router, Top1MoELayer, routing masks, load histograms, and optional capacity handling. |
| 5 | Part II - routing mechanics from scratch | Top-2 routing and weighted expert combination | Generalize sparse routing so each token can combine two expert transformations. | Implement Top2Router and Top2MoELayer with normalized gates, duplicate-safe dispatch, and comparison diagnostics. |
| 6 | Part II - routing mechanics from scratch | Vectorized dispatch, capacity, and routing efficiency | Move from readable loops to scalable tensor operations without changing the MoE math. | Implement vectorized dispatch utilities, capacity-aware routing, and correctness tests against naive top-2 output. |
| 7 | Part III - training routers and experts | Router training and load-balancing losses | Teach the router to avoid expert collapse before moving to auxiliary-loss-free balancing. | Implement auxiliary load-balancing loss, optional router z-loss, entropy diagnostics, and trainer integration. |
| 8 | Part III - training routers and experts | Shared experts and fine-grained routed experts | Implement the DeepSeekMoE-inspired expert structure for the reference model. | Implement SharedExpertMLP, FineGrainedExpertBank, and MiniDeepSeekMoELayer with shared plus routed outputs. |
| 9 | Part III - training routers and experts | Auxiliary-loss-free load balancing | Replace auxiliary balancing with a router-bias mechanism that affects selection but not combine weights. | Implement router_bias state, biased top-k selection, unbiased gate computation, and no-gradient bias updates from observed load. |
| 10 | Part IV - assembling and training the model | Assembling MiniDeepSeekMoE end to end | Integrate the final MoE layer into a complete decoder-only language model. | Produce the final model.py, config.py, train.py, sample.py, and tests for the MiniDeepSeekMoE implementation. |
| 11 | Part IV - assembling and training the model | Training, diagnostics, and controlled experiments | Use the model to answer concrete questions about routing, balance, and sparse capacity. | Build experiment scripts, diagnostic plot generation, and ablation tables for dense versus MoE variants. |
| 12 | Part V - inference and scale | Inference, performance, and scaling beyond the toy model | Connect the from-scratch implementation to systems concerns without turning the book into a distributed-systems text. | Add inference helpers, active-compute estimators, simple profiling scripts, and conceptual scaling diagrams. |
