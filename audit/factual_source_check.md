# Factual source check

Phase 7 did not introduce new external research claims. The final package preserves the Phase 3 framing:

- MiniDeepSeekMoE is a DeepSeekMoE-inspired teaching model, not a literal DeepSeek-V3 clone.
- Top-1 and top-2 routing remain chapter milestones, not the final architecture by themselves.
- Router bias affects expert selection only.
- Final combine weights are computed from unbiased selected scores.

No factual changes were made to the model choice during Phase 7.
